export enum InitiativeGoalType {
  Connect = 1,
  Learn = 2,
  Build = 3,
  Play = 4,
  Explore = 5,
}

export enum InitiativeStatus {
  Idea = 1,
  Active = 2,
  Completed = 3,
  Archived = 4,
}

export enum InitiativeVisibility {
  Public = 1,
  Private = 2,
}

export enum InitiativeJoinRequestStatus {
  Pending = 1,
  Accepted = 2,
  Rejected = 3,
}
